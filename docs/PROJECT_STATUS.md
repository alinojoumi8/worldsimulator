# Project Status

Last updated: 2026-07-16.

## Current phase

Phases 0–7 are complete and the PRD §28 MVP gate is accepted through WS-710. The persistent 100-person Riverbend world now covers deterministic agents, double-entry money, contracts, companies, labor, production, posted-price markets, energy billing and shocks, insolvency, complete credit, bounded MiniMax/Kimi decisions and conversations, cited news, sentiment, all ten economic series, checksummed exports, strict/observe replay, why-panels, controls, and a production-shaped browser path. The exact seed-42/mock acceptance run reaches tick 360 inside every envelope, passes every active invariant, strictly replays with zero divergences, and matches both terminal logical-state and raw event-log hashes in a second independent run. Built-in mock and cache-only latency are deterministic; live and injected providers retain measured telemetry. V1 now begins at WS-801. External citizen tools, connectors, and real accounts remain outside MVP, V1, and Phase 11.

Completed foundation work includes the strict pnpm/TypeScript monorepo, deterministic money/RNG/codec primitives, event envelopes, synchronous event bus, ordered simulation loop, mock LLM provider, Fastify health/version endpoints, and the cross-platform determinism gate.

The Phase 6 acceptance artifacts now have shared strict schemas and semantic
validators. `pnpm gate:phase6` is the authoritative phase boundary: it
independently verifies both canonical digests and their causal, monetary, hash,
ordering and checklist claims, and fails closed if either live artifact is
missing or inconsistent.

## Phase 1 progress

- **WS-101 implemented:** `better-sqlite3` wiring, safe per-run paths, WAL and foreign-key configuration, safe-integer mode, checksummed idempotent migrations, and the initial runs/events/snapshots/scheduler schema.
- **WS-102 implemented:** durable run-scoped event storage, atomic batch append, gapless sequence enforcement, database triggers blocking update/delete, deterministic filters/keyset pages, bigint payload round-trips, and stable log hashing. The local 10,000-event benchmark is below the 100 ms target.
- **WS-103 implemented:** `TickUnitOfWork` now wraps the complete phase pipeline, scheduler effects, staged events, and run checkpoint in one root `BEGIN IMMEDIATE` transaction. `currentTick`, event sequences, and ID state advance only after commit; injected failures before and after event insertion roll everything back for a gapless retry.
- **WS-104 implemented for the empty-world vertical slice:** the restart-safe run repository, immutable manifest, guarded lifecycle transactions, continuous run supervisor/recovery, manual stepping, end-tick completion, and lifecycle tests are in place.
- **WS-105 implemented:** the persisted scheduler validates and canonically stores tasks, orders them by `(dueTick, order, id)`, and fires them through the `M01-scheduler` obligations handler at order 0. The service wires an explicit dispatcher; unknown references and throwing handlers fail the tick and leave tasks pending.
- **WS-106 implemented:** online backup-API snapshots run after each configured N-tick boundary (default 100), finalize immutable metadata plus paired state-hash/snapshot events into both the backup and live database, fsync + atomically rename, repair a missed post-commit boundary before the next tick, and reject concurrent run mutations while backup is in flight. Restored checkpoints reproduce the exact state hash and event log after equivalent advancement; crash cleanup and multi-boundary sync/async paths are covered.
- **WS-107 implemented:** successful create/control/advance mutations journal `admin.command.received` before their effects; rejected mutations append nothing. Lifecycle facts, event IDs, and run checkpoints commit together.
- **WS-108 implemented for the empty-world slice:** strict shared request/response schemas and real Fastify routes cover create/list/detail, start/pause/resume/stop, synchronous and durable restart-safe asynchronous advance, task polling via status, descending filtered events, opaque cursors, and RFC 9457 errors.
- **WS-109 implemented at v0:** the SSE adapter polls only committed SQLite rows and provides Zod-validated digest/lifecycle/gap frames, strict `Last-Event-ID` resume, heartbeat, bounded gap frames, backpressure handling, and clean shutdown. The dashboard uses an authenticated fetch stream with bounded exponential retries; a 401 suspends reconnects and a gap refreshes durable REST queries. News/error topics arrive with their owning modules.
- **WS-110 implemented:** Pino JSON logging, secret redaction, run/tick/correlation bindings, production Fastify integration, and non-authoritative per-phase timing metrics are implemented. Success, rejection, asynchronous-task, restart-recovery, and automatic-worker failure paths are asserted. An unhandled tick error rolls back, pauses the run, and records a durable error event; kill-mid-tick and restart recovery gates close Phase 1.

## Phase 2 progress

- **WS-201 implemented:** strict agent/persona/household/goal/relationship schemas, deterministic IDs, 14 skills, 40 role-complete occupations, synthetic names, and public-figure blocklist enforcement.
- **WS-202 implemented:** Riverbend generates its exact 100-person roster through deterministic named RNG streams, satisfies the hard age/education/income/household/finance/credit/social-graph/storyline gates, records spec and population hashes, and aborts rather than returning a partial invalid world.
- **WS-203 implemented:** immutable append-only memory records, deterministic integer salience scoring and top-k retrieval, bounded template compaction, source/reference preservation, SQLite reopen behavior, and memory-backed decision observations are complete. Memory text remains untrusted data.
- **WS-204 implemented:** all eight trigger kinds have strict contracts plus deterministic due/stale filtering, deduplication, priority ordering, known-agent checks, per-agent caps, and wake-set hashes.
- **WS-205 implemented:** the typed action registry is the validation and execution choke point for schema, capability, domain, and executor checks. Adversarial JSON properties verify that rejected input cannot reach an executor.
- **WS-206 implemented:** Tier 0 routines, complete Tier 1 utility tables, typed decision/action records, stable selection, no-op fallback, caps, failure isolation, quarantine cooldowns, work counters, and a twice-run 360-tick Riverbend determinism gate are in place.
- **WS-207 implemented:** production simulation creation persists all Riverbend Phase 2 entities. Strict agent directory/profile/relationship/decision routes provide run-bound opaque pagination, filters/search, memory highlights, and explainability records. A real API test covers tick execution, malformed/404 paths, snapshots, restore, reopen, and same-seed equality across different run IDs.
- **WS-208 implemented:** deterministic activation rules and active-goal caps feed goal triggers into the decision phase; optimistic progress/achievement/abandonment transitions cannot commit without authoritative events.
- **WS-209 implemented:** the test-only INV-1..10 checker reports passed/failed/inactive status and catches a seeded violation for each invariant. Authoritative Phase 3 records activate INV-1..6 and INV-8..10; only the V1 market invariant INV-7 remains inactive.
- **Live-provider contract verified against real Riverbend context:** an authenticated structured-output call used Della Ashcombe's generated profile, active goal, and retrieved memory plus an adversarial untrusted-memory instruction. The model selected the registry-valid high-utility no-op, the invalid control fell back deterministically, and usage/API-equivalent cost were captured. The first transport attempt also documented that top-level `oneOf` is unsupported. See [Live provider contract](LIVE_PROVIDER_CONTRACT.md).
- **Live Phase 2 runtime verified:** the real TCP smoke generated 100 agents, 198 goals, 600 directed relationships, 100 tick-1 decisions/actions, and 200 memories; all active invariants passed, the snapshot hash matched live state, and the decision feed survived service reopen. See [the captured smoke artifact](../artifacts/live-phase2/2026-07-15-smoke.json).

Phase 2 is closed. The v9 logical state hash includes every authoritative Phase 2, Phase 3, and WS-401–WS-408 table while excluding run/simulation/wall-clock identity; equal deterministic state produces equal canonical hashes across run identity and snapshot restore. WS-409 is read-only and deliberately leaves the authoritative hash version unchanged.

## Phase 3 progress

- **WS-301/302 implemented:** First Ledger Bank, immutable owner-scoped accounts, outbound capability checks, explicit floors, an immutable multi-leg journal, exact debit/credit validation, authorized money channels, idempotency, balance caches, and reconciliation.
- **WS-303 implemented:** all resident/company/treasury balances are real mint postings from zero; seeded loans have bank-asset/liability links; six founder cap tables sum exactly; the opening state passes INV-1..6.
- **WS-304/305 implemented:** D15/D30 payroll splits annual cents exactly over 24 periods, applies HALF_EVEN withholding, posts net/tax/gross legs, emits missed payroll atomically, records append-only effective policies, and suspends benefits before treasury overdraft.
- **WS-306/307 implemented:** households settle food, utilities, rent, and propensity-based discretionary demand in priority order against exact available funds. Reference-priced ROW SKUs and all external inflows/outflows are explicit `row_settlement` transactions; unmet essentials emit financial stress.
- **WS-308/309 implemented:** M1, average wage, unemployment, and treasury series persist at genesis and every tick. Filtered transactions, bank list/detail, indicators, and agent-finance endpoints use shared contracts; committed digests carry the real latest indicator set.
- **Release gate implemented:** a 360-day run reconciles all account caches, records 1,444 indicator points and snapshots at ticks 120/240/360, completes all 360 tick commits, and passes every active invariant. A Phase 3 snapshot restores to the identical v3 state hash and reconciled books.

WS-310 closes Phase 3. The React 19/Vite/TanStack Query cockpit requests the bounded indicator API and plots M1, treasury balance, average wage, and unemployment in responsive Finance and Employment panels. Values remain integer-safe, empty series stay explicit, and the page retains lifecycle controls, status/tick/date/progress, SSE connection state, digest counts, the event ledger, and token-in-session handling.

## Phase 4 foundation progress

- **WS-401 implemented:** strict incorporation/employment/service/lease contracts, all-party signatures, the exact legal state machine, effective-tick activation, deterministic obligations, recurring lease cutoff, overdue breach predicates, and append-only timelines.
- **WS-402 implemented:** signed incorporation drives fee payment, registration, business-account opening, founder capital, exact equity, and activation one stage per tick. Pre-activation hire/trade and same-tick job posting are rejected. Formation ledger postings link to committed causal events.
- **WS-403 implemented:** active-company job postings and applications use deterministic Tier-1 eligibility and score ordering. A selection creates an active, fully signed legal employment contract; quit and layoff paths enforce authority and notice before effective termination.
- **WS-404 implemented:** the immutable seven-SKU catalog and company production profiles drive integer, capacity-bounded production from active labor. Inventory can never become negative. Immediate posted-price orders revalidate buyer ownership, funds, price, and stock; a filled order atomically posts its balanced purchase transaction and sale movement, while shortages persist rejected orders and stockout events. Household grocery demand buys from real sellers first and uses ROW only for the remainder.
- **WS-405 implemented:** every offering reviews its trailing seven ticks weekly. Stockouts and low inventory cover raise price; excess or unsold inventory lowers it; balanced/no-activity windows hold. Five-percent steps and founder decisions share the exact integer cost-to-50-percent envelope. Founder authority and current decision provenance are checked, immutable price history records rule evidence, and every actual change emits `market.price.updated` with its old/new price and cause.
- **WS-406 implemented:** Riverbend Power & Light charges one flat household bill per 30-tick cycle and per-unit business bills from actual production. The active business tariff is included in produced inventory cost. Paid deliveries generate exact fuel requirements and balanced ROW fuel purchases; insufficient customer funds create immutable rejected bills without partial payment. Fuel shocks are bounded and update household/business tariffs only at the next cycle using the versioned 60% pass-through rule. Migration v9, state-hash v7, event causality, rollback/reopen, and snapshot/next-shock equivalence are covered.
- **WS-407 implemented:** Every active company receives an immutable daily cash-versus-30-tick-obligation assessment. Thirty consecutive shortfall days atomically terminate staff and contracts, withdraw jobs, deactivate offerings, salvage inventory to ROW at 50% of reference price, pay immutable claims by employee/secured/tax/trade/unsecured seniority, write off exact deficits, distribute only true surplus through the founder residual tier, close zero-balance accounts, and emit `company.failed` with the full cause chain. Migration v10, state-hash v8, INV-5 terminal post-conditions, rollback/reopen, and snapshot/next-day liquidation equivalence are covered.
- **WS-408 implemented:** The strict v1 catalog accepts only energy, ROW-price, demand, and business-capacity shocks while a run is created/paused. Every accepted injection journals its admin command and injection before a durable future-boundary task. Applied events delegate to owning modules with complete causality; demand/disaster windows and overlaps are bounded, energy pass-through raises later tariffs and firm cost, and ROW/demand changes reach household evidence. Migration v11, state-hash v9, HTTP/catalog validation, task/effect rollback, reopen, and snapshot/next-boundary equivalence are covered.
- **WS-409 implemented:** Strict shared schemas and run-bound cursors expose company, contract, job, institution, and goods-market resources. Company detail joins cap tables, staff, offerings, jobs, trailing financials, solvency, wind-down evidence, and a condensed causal timeline without mutating state. The React World Explorer adds list/detail routes plus citizen employment/finance views; real HTTP responses are contract-parsed and component behavior is covered.
- **Release gate implemented:** a production `SimulationService` run completes 360 ticks with a surviving producer, a deliberately undercapitalized employer that fails after exactly 30 shortfall days, an approved tick-8 fuel shock that raises tick-30 tariffs, real household/business energy bills, snapshots at ticks 120/240/360, reconciled accounts, and all active invariants. The same read model used by API/UI explains activation, hiring, production, repricing, insolvency, employment termination, and final wind-down.

The Phase 4 persistence migrations add legal contracts/parties/obligations/executions/breaches/timelines, companies/equity/timelines, jobs/applications, employment terminations, the product catalog, offerings, production profiles/runs, inventory/movements, orders, stockouts, immutable price and energy history, bills/fuel purchases, daily solvency assessments, creditor claims/recoveries/write-offs, inventory salvages, wind-down summaries, WorldEvents, ROW-price history, demand shocks, and capacity disasters. WS-409 adds deterministic read projections only; it introduces no migration or authoritative state.

## Phase 5 progress

- **WS-501 implemented:** A submitted personal or company application receives a deterministic model-v1 score from stored income stability, conservative DTI, and payment history. The assessment records the exact cents inputs, observations, derived basis points, no-history flag, component points, and source evidence IDs. Applications and assessments are immutable, their two events share correlation and direct causation, migration v12 persists them, and logical state hash v10 includes them. Goldens, properties, malformed input, neutral no-history, transactional rollback, reopen, and snapshot/next-application equivalence are covered.
- **WS-502 implemented:** Submitted applications receive an immutable review assignment from the least-loaded active First Ledger loan officer. Rule-only review applies a zero-point adjustment with a written rationale, then evaluates six stable model-v1 checks: minimum score, maximum DTI, maximum term, borrower exposure, active bank status, and current minimum capital ratio. Approval alone receives a deterministic risk-priced offered rate. SQL guards require matching score/review/decision rows and block authority or transition bypasses. Migration v13, state-hash v11, complete causal approved/rejected events, rollback/reopen, and snapshot/next-decision equivalence are covered. WS-505 now supplies the live reserve/capital/concentration evidence consumed by this decision boundary.
- **WS-503 implemented:** Approved applications receive immutable equal-principal schedules using exact 30/360 periods, canonical HALF_EVEN interest rounding, and a final principal row that absorbs every residual cent. Disbursement is one SQLite unit of work: open the bank asset, credit both the asset and borrower deposit, offset them through the bank loan-source liability, append the causal loan/account/schedule/transaction events, and persist the loan plus all installments. SQL requires the exact approved terms and three-leg ledger link, live loans feed subsequent score inputs, INV-6 sees both seed and originated loans, migration v14 and state-hash v12 include the new state, and rollback/reopen plus snapshot/next-disbursement equivalence are covered.
- **WS-504 implemented:** The M09 order-60 obligations handler evaluates exact due ticks after payroll and benefits. A borrower must fund the whole ordered arrears set; successful collection posts one immutable balanced transaction per installment, books interest separately, completes missed/current rows together, reduces principal exactly, and resets the streak. Insufficient funds mark only the current row missed. The third consecutive miss emits the complete history, leaves legal principal visible, writes the loan asset to a dedicated internal expense account, persists an immutable default record, and applies a 100-point agent score penalty with a 300 floor. Migration v15, state-hash v13, SQL transition and ledger-shape guards, rollback/reopen, and snapshot/next-collection equivalence are covered.
- **WS-505 implemented:** Every approval and disbursement receives an immutable circuit assessment over current and projected domestic deposits, fixed reserves, retained-income/loss-adjusted effective capital, and bank-specific borrower exposure. Riverbend opens at 18% reserves and 14% capital with explicit 12% and 10% minimums. A projected systemic breach journals the evidence and transitions the bank to `lending_halted`; a safe projected request resumes it. Exposure-cap failures remain borrower-scoped. Disbursement rechecks stale approvals before creating an account, transaction, loan or schedule. Migration v16, state-hash v14, authoritative SQL arithmetic/provenance guards, stress boundaries, rollback/reopen, and snapshot/next-assessment equivalence are covered.
- **WS-506 implemented:** The opening portfolio is audited as one deterministic state: Ironvale's 36-month loan is current at month 22 with $116,666.62 outstanding; six distinct personal borrowers are current; one additional personal borrower has exactly one missed payment. The shared audit recomputes every equal-principal row, exact HALF_EVEN 30/360 interest amount, seasoned status, outstanding balance, borrower account, bank asset, recognition transaction, and `loan.seeded` causal fact. Migration v17 validates canonical rows and recognition links and makes seed loans immutable. INV-6 now consumes this complete opening audit; failed writes, reopen, state-hash stability, and snapshot restore are covered.
- **WS-507 implemented:** Two new immutable indicator series expose gross contractual credit outstanding and the all-loan recorded-default rate. The strict credit read model joins opening and originated loans into one deterministic, filterable, opaque-cursor feed. Opening details show only stored recognition/schedule/event provenance; originated details show the complete stored application, exact score inputs and breakdown, review, six policy checks, both circuit assessments, schedule, default record and evidence chain. Bank detail derives its trailing 30-tick interest/write-down statement from ledger legs. Migration v18 expands the indicator constraint; real-route contract tests, non-zero collection/default assertions, UI component coverage, reopen and snapshot restore prove UC-4 without synthetic data.
- **WS-508 implemented:** The reusable pure M26 report folds every ledger leg by tick, reconstructs both accepted M1 v1 and treasury, and reconciles them to all 361 persisted point pairs. Material domestic-supply deltas must use mint, lending, repayment or ROW and every transaction must have exactly one same-tick/same-kind `transaction.posted` fact. Treasury-excluded M1 is explained by the exact supply-minus-treasury identity, so payroll withholding and benefits remain fiscal reclassifications. INV-2 now receives real signed domestic deltas. The 360-day sweep reaches 10,000 bp attribution with identical material/evented supply counts and zero gross or net unattributed cents; adversarial tests catch bypasses, missing evidence and indicator drift.

## Phase 6 progress

- **WS-601 implemented and provider routing corrected:** The dependency-free OpenAI-compatible transport supplies strict `MiniMaxLlmProvider` and `KimiLlmProvider` adapters. New manifests pin Tier 2 to `MiniMax-M3` and Tier 3 to Kimi K2.6 or explicit K2.7 Code. Kimi receives native strict JSON Schema; MiniMax receives a strict schema-in-prompt contract. Both parse one JSON value, revalidate with Zod, permit one repair without replaying hostile output, validate usage, map stable HTTP/transport failures, expose no tools and can only return proposals. Eight focused tests cover provider request shapes, Bearer-key isolation, repair, cached tokens, K2.7 thinking behavior, error mapping and tier routing. The prior Anthropic adapter and its tests remain intact for legacy manifests. See [provider evidence](WS_601_MINIMAX_KIMI_PROVIDERS.md).
- **WS-602 implemented:** `llmRequestHash` and the route-aware composite key canonically bind every output-shaping request field, provider, model, prompt-pack version and schema version while excluding causal telemetry. `CachedLlmProvider` supplies read-through and hard cache-only modes; a replay miss makes zero provider calls, and corrupt rows, hash drift, schema drift, audit faults and write faults remain typed no-throw fallbacks. Migration v19 stores immutable run-scoped responses and a separate gapless `llmce_*` audit stream so preload cannot consume simulation IDs. Every audit fact carries version, system actor, tick, correlation, causation and evidence. Canonically ordered SHA-256 artifacts reject tampering before atomic idempotent import. Injected rollback, trigger immutability, reopen, state-hash neutrality, and exact snapshot/restore tests cover the persistence boundary. See [WS-602 evidence](WS_602_RESPONSE_CACHE.md).
- **WS-603 implemented:** `BudgetedLlmProvider` wraps the cache/provider boundary with exact integer-microcent charging for uncached input, provider-cached input and output, isolated per-agent simulated-day tokens, the Tier 3→2→1 80/100 ladder, unpriced-model fail-closed behavior and zero further calls after exhaustion. A WorldTangle response-cache hit remains free. Migration v25 adds authoritative cached-input counters and monotonic `cached <= input` guards to the v20 control state; state-hash v19 and snapshots preserve them. Usage and all threshold/pause facts share the main journal transaction. Exact cached-price, forced-low-budget, cache, rollback, reopen, API and snapshot-equivalence suites cover FR-ADM-1/2. See [WS-603 evidence](WS_603_LLM_BUDGETS_CONTROLS.md).
- **WS-604 implemented:** `PromptPackRegistry` resolves immutable exact versions and ships the manifest-aligned `agent.decision@1` pack. `AgentObservationBuilder` canonicalizes trusted state, puts bounded memory/message/news prose inside one content-derived collision-free fence, and renders only sorted strict engine options after the fence. The stable system prefix contains scenario Persona data and explicit no-tools/no-authority rules. Prompt, observation and full request identities have hard goldens; Tier-2/3 Decisions require the pack key/version/hash and expose them through the decision API, while Tier-1 rejects prompt metadata. A real Riverbend Persona/memory record survives canonical persistence, state hashing, snapshot restore, API read and reopen unchanged. No migration or state-hash bump is required. See [WS-604 evidence](WS_604_OBSERVATIONS_PROMPTS.md).
- **WS-605 implemented:** The runtime discovers canonically ordered founder-price, founder-hiring, applicant-response, loan-review and goal-activation opportunities before the tick, passes their registered prompts through the same mock/live cache and budget gateways, then applies the prepared batch synchronously inside the authoritative tick unit of work. Exact offered action type and canonical parameter equality is the hard capability boundary. Hiring requires current persisted founder and applicant decisions; pricing remains inside integer cost bounds; loan adjustment is exactly -5..+5 with stored rationale and Decision provenance; goal selection is revalidated at apply time. Provider, hash, schema, budget, or action-validation failures deterministically fall back to Tier 1 and cannot apply the rejected proposal. Migration v21 adds append-only `llm_call_records` and the loan Decision link; state-hash v16 and snapshots include both. A validation-rejected provider response is recorded as `validation_failed`, effective Tier 1, with real attempt/token evidence retained. See [WS-605 evidence](WS_605_TIER2_DECISIONS.md).
- **WS-606 implemented:** Two-party purchase/job conversations use canonical pre-tick Tier-3 message and Tier-2 outcome preparation plus synchronous transactional apply. Shared and SQL contracts enforce six turns, 4,096 aggregate output tokens, one opportunity per agent per tick, a seven-tick same-topic cooldown, exact alternation, next-tick inbox delivery/read state, structured-term bounds, structural acceptance, and no-progress closure. Free text remains fenced and non-binding; exact option/param, speaker, tick, request-hash, call, term, and token checks run again before persistence. Failures deterministically decline or close without mutation. Migration v22 adds immutable conversations, messages, inboxes, and relationship history; state-hash v17, causal events, two-way bounded relationship updates, memories, rollback/reopen, and snapshot equivalence are covered. See [WS-606 evidence](WS_606_BOUNDED_CONVERSATIONS.md).
- **WS-607 implemented:** Terminal purchase/job conversations bind once in canonical order only after the coordinator and owning domain store independently recheck the exact structured terms, participant authority, terminal evidence, and current economic state. Purchases revalidate the active offering/company, cost-to-posted-price bounds, inventory, buyer account and funds before one exact-cents order/ledger/inventory commit. Jobs revalidate the submitted top-ranked application, active company, live vacancy and reservation-to-advertised wage bounds before one fully signed employment contract. Misleading prose is ignored; stale state records an immutable rejection. Migration v23 adds immutable causal binding results, state-hash v18 includes them, and idempotence, rollback/reopen and snapshot equivalence are covered. See [WS-607 evidence](WS_607_NEGOTIATION_BINDINGS.md).
- **WS-608 implemented:** Migration v24 extends immutable LLM call receipts with nonnegative operational latency and exact integer-microcent cost; migration v25 adds cached-input usage to authoritative budget rows. Timing observations remain snapshot-preserved but hash-neutral. `llm.call.recorded` v2, `/llm-calls`, `/status` and the React observability view now expose input/cached-input/output tokens, attempts, latency, exact cost and cache evidence. Strict error and conversation projections retain causal failures, quarantines, inert transcripts, structured outcomes and bindings. Reopen, snapshot, cursor, route/client, cached-price reconciliation and hostile-content coverage are green. See [WS-608 evidence](WS_608_LLM_OBSERVABILITY.md).
- **WS-609 complete:** The refreshed real seed-42 loopback run exercised authenticated MiniMax and Kimi routes with a calibrated nonbillable 200-cent reference table. Two upstream attempts across 28 receipts reported 2,142 input, 882 cached-input and 360 output tokens; exact recorded and independently repriced spend both equal 474,606,000 microcents. The bounded overshoot auto-paused at tick 301, its threshold/pause event chain is causal, and provider attempts remained 2 after the grace window. Artifact digest: `913445f696aab6994ec6448a378b1e4f6b6695e14c61be5d7ff9fca440e17b5f`. See [WS-609 runbook](WS_609_LIVE_BUDGET_ACCEPTANCE.md).
- **WS-610 complete:** A refreshed real MiniMax M3 proposal and its mock replay have equal canonical request identity and equal provider-neutral call, Decision, AgentAction, same-tick event and affected-agent projections. All five section digests match; proposal hash is `f1ff27bdfa771150636b7e563cb968a4552d370efd01836c218b9d5f12536284` and evidence digest is `07eaceedf30bdc616ae14c87ac2c1d4fce1bf13bcc5e6f93bedc6b31014f6db0`. See [WS-610 evidence and runbook](WS_610_LLM_PARITY.md).
- **Phase 6 gate passed:** `pnpm gate:phase6` independently recomputed both canonical evidence digests, exact WS-609 pricing/tolerance/rounding and pause causality, both WS-610 request hashes, its self-contained proposal hash and all five ordered parity-section digests. Phase 7 is unblocked.

## Phase 7 progress

- **WS-701 implemented:** The engine builds a versioned per-tick candidate digest from immutable journal events. Integer-only scoring combines the largest absolute cents fact, inverse event-type frequency over an inclusive lookback, and deduplicated or explicit affected entities. Operational audit families never enter the digest. Ranking is input-order independent with code-unit tie-breaks; candidate and digest hashes exclude wall time and run/request identity while exact persisted facts retain them. Pre-aggregated occurrence counts are result-equivalent and fail closed on duplicates or undercounts. Nine focused tests lock scoring, ranking, hash, filtering, lookback, identity-neutrality and validation. See [WS-701 scoring contract](WS_701_NEWSWORTHINESS_DIGEST.md).
- **WS-702 implemented:** The Riverbend Ledger validates one editor and assigned staff journalists, records a curated digest every tick, and opens deterministic 30-tick editorial windows with a three-story daily cap and distinct-topic dedupe. The engine owns complete neutral/context/brief drafts with exact committed event facts; Tier 2 sees only fenced content plus exact draft ID/hash choices. Off mode uses the neutral template without a call. Invalid provider, schema, hash or menu results are durably spiked with zero reach and never publish. Migration 26 adds immutable organization/digest/story/citation rows; logical state-hash v20, equal-seed identity neutrality, rollback/reopen and snapshot restore equivalence are covered. The 360-tick gate proves at least 12 valid publications. See [WS-702 story contract](WS_702_STORY_PIPELINE.md).
- **WS-703 implemented:** Three integer public indices use exact per-tick decay plus bounded stance, reach, and cited-outcome effects. Sparse story-boundary rows retain exact lazy-decayed reads. Persisted reach deterministically samples exposed agents; append-only opinion rows and cause links enforce a five-point agent/axis/tick cap, while one ordered batch event preserves every transition without journal amplification. Later Tier-1 decisions store the exact public/opinion evidence and can receive only opposing ±25 response/no-op utility deltas. Migration 27, logical state-hash v21, restore/reopen, immutability, forged-input rollback, attribution, and a complete 360-tick Phase 4 run are green. See [WS-703 sentiment contract](WS_703_SENTIMENT_ENGINE.md).
- **WS-704 implemented:** The metrics boundary now atomically persists all ten FR-OBS-2 series. CPI is a base-1000 fixed expenditure-share basket with exact rational aggregation and one HALF_EVEN rounding; GDP proxy is the trailing 30-tick sum of final domestic goods and paid household-energy expenditure; active businesses are unique active company checking owners; and sentiment is the effective mean of economy, employment, and institutions. The existing M1, wage, unemployment, credit, default, and treasury formulas share the same ruleset-v1 evidence boundary. Each immutable point and `economic.metrics.updated` fact carries a canonical source-input digest. Migration 28 retains old rows as explicit version-0 evidence, logical state-hash v22 covers value/version/digest fields, and formula, shock, API/UI, migration, atomicity, reopen, and restore-equivalence suites are green. See [WS-704 indicator contract](WS_704_FULL_INDICATORS.md).
- **WS-705 implemented:** Terminal runs now replay asynchronously into a fresh run from their version-pinned manifest, ordered admin journal, and checksum-validated LLM cache. Lifecycle, advance, approved world-event, and LLM-control commands are reapplied at their original tick boundaries; every deterministic event projection is compared in sequence and the terminal logical state hash must match. Replay reconstructs source provider/cache/attempt/token/fallback evidence in hard cache-only mode and makes zero live calls. Strict mode halts at the first typed divergence; observe mode records and continues through deterministic fallback. Migration 29 stores immutable hash-neutral replay summaries, divergences, and LLM expectations with guarded status transitions. Restart recovery, injected-shock equivalence, missing-cache containment, causal mismatch detection, API/status contracts, rollback/reopen, and snapshot preservation are green. See [WS-705 replay contract](WS_705_REPLAY_EXECUTOR.md).
- **WS-706 implemented:** Terminal or paused runs can queue canonical JSONL or deterministic fully quoted CSV for events, transactions, and indicators. Every job pins its source tick and logical state hash, runs under the normal per-run operation lock, writes content-addressed UTF-8 files atomically, and publishes a checksummed canonical manifest with the exact simulation disclaimer. Migration 30 stores hash-neutral jobs, immutable file facts, and gapless versioned audit events with actor/correlation/causation/evidence. Queued and running jobs recover after restart. Shared contracts reject mismatched status, causal chains, datasets, paths, formats, or hashes. API, Windows flushing, upgrade, rollback/reopen, snapshot preservation, logical-hash neutrality, JSONL/CSV round trip, checksum/byte/row reconciliation, exact account-balance reconstruction, and worker restart recovery are green. See [WS-706 export contract](WS_706_EXPORT_JOBS.md).
- **WS-707 implemented:** Published-story feed/detail contracts now enforce bounded queries, run-bound pagination, resolved author/newsroom identity, canonical sentiment series, exact ordered citations, and reconciled impact evidence. The new News Explorer renders inert cited stories and accessible sentiment sparklines, filters full committed event and balanced-transaction ledgers, follows immutable `causationId` ancestry, and drives strict/observe cache-only replay with target stepping and divergence progress. A cited story reaches its causal event within two clicks. Shared, cursor, route, real Fastify/SQLite, hostile-content, causal-ledger, and replay-stepper tests are green. See [WS-707 UI contract](WS_707_NEWS_EXPLORER_UI.md).
- **WS-708 implemented:** The built dashboard and real Fastify/SQLite server now pass a one-worker Chromium path that creates a seed-42 mock simulation, uses lifecycle controls, opens a citizen and real loan why-panel, schedules an approved 30% fuel-price shock, resumes to tick 31, and observes CPI. A new cockpit injector exposes only the four bounded WS-408 catalog entries and requires a paused run. Playwright uses an isolated temporary database, retains trace/screenshot/video evidence on failure, and runs in the Windows and Ubuntu CI matrix without live-provider credentials. See [WS-708 acceptance contract](WS_708_PLAYWRIGHT_ACCEPTANCE.md).
- **WS-709 implemented:** The exact `riverbend-100@1`, seed-42, mock, tick-360 contract now has a versioned canonical envelope and authoritative SQLite probe. The gate requires every tick 0–360, all INITIAL_WORLD §9 bounds, unique outcome evidence, exact M1 attribution, finance reconciliation, active invariants, snapshots, and a terminal logical hash. The measured run holds unemployment at 649 bp and CPI at 1000, forms three active companies from achieved founder goals, records one insufficient-funds deferral, and has zero business failures, loan defaults, or benefit suspensions. A cross-phase slot reservation repaired the former tick-331 action-cap overflow, and the invariant probe recognizes all engine-validated Tier-2 identity fields. See [WS-709 evidence](WS_709_SCENARIO_REGRESSION.md).
- **WS-710 implemented:** `pnpm gate:mvp` executes the complete PRD §28 acceptance matrix. The source/replay/second-source 360-tick gate now proves exact logical-state and raw journal identity, while focused tests lock causal LLM expectation order, pre-commit replay cursors, replay-neutral prompt/news projections, deterministic built-in mock telemetry, and cost-sensitive factual hashes. Phase 6 budget evidence, all active invariants, formation-to-loan-to-operations, energy-shock propagation, valid cited news, reconciled exports, adversarial rejection, global simulation labeling, and the cross-platform browser path are green. See [WS-710 evidence](WS_710_MVP_ACCEPTANCE.md).

WorldTangle's causal-tapestry identity is implemented in `apps/web/public/brand/`. GPT Image 2 sources, the selected Causal Knot concepts, prompt manifest, accessibility guidance, and deterministic export script live in `design/brand/`.

External streams/read models must consume committed events from the persistence boundary; the in-process event bus is for deterministic pre-commit module coordination.

## Verification gate

Before each handoff, run:

```bash
pnpm typecheck
pnpm lint
pnpm test
pnpm build
```

Latest verified gate (2026-07-16): `pnpm gate:mvp` passed strict type-check, lint, both Phase 6 artifact validators, all 127 Vitest files (676 tests), the production web build, and the real one-worker Chromium happy path. WS-701–710 coverage includes scoring and decay goldens, input-order stability, exact citations, deterministic reach exposure, complete opinion/prior attribution, all ten economic formulas, exact CPI shock propagation, replay event/state equivalence, causal LLM expectation ordering, pre-commit replay cursors, replay-neutral prompt/news facts, deterministic built-in mock telemetry, cache-only live-shaped evidence, strict/observe divergence handling, migration 30, atomic rollback, reopen, logical state-hash v22, snapshot preservation, checksummed JSONL/CSV exports, authoritative row-count and account-balance reconstruction, restart recovery for in-flight export jobs, published-news API integration, resolved feed parties, run-bound pagination, accessible sentiment sparklines, hostile-text containment, event/transaction why-panels, causal ancestry, replay-stepper progress, bounded shock-injector authority, the seed-42 create-to-loan-to-shock-to-CPI browser path, and exact two-run 360-tick state/journal hashes. The Phase 3 finance-isolation and complete Phase 4 company/shock/failure/news/sentiment/indicator gates remain green. Fresh authenticated MiniMax/Kimi acceptance and MiniMax parity artifacts pass `pnpm gate:phase6`. The suite uses two Vitest workers to avoid Windows worker-RPC timeouts during the CPU-heavy 360-tick gates. The build succeeds with a 579.39 kB chunk-size advisory. The 10,000-event append benchmark was last verified on 2026-07-14 at 99.85 ms against the 100 ms target.

Next: implement WS-801, VC entities and exact fund accounting.
